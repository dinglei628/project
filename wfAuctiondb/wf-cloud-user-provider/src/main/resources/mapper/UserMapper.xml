<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zb.mapper.UserMapper">
    <select id="userLogin" resultType="com.zb.entity.AuctionUser">
        select * from auctionuser where userName = #{userName} and userPassword = #{userPasswrod}
    </select>

    <insert id="addUser" parameterType="com.zb.entity.AuctionUser">
        INSERT INTO auctiondbuser.auctionuser
	(
	userName,
	userPassword,
	userCardNo,
	userTel,
	userAddress,
	userPostNumber
	)
	VALUES
	(
	#{userName},
	#{userPassword},
	#{userCardNo},
	#{userTel},
	#{userAddress},
	#{userPostNumber}
	);
    </insert>

	<select id="selAuctionUserById" resultType="com.zb.entity.AuctionUser">
		select * from AuctionUser where userId=#{userId}
	</select>
</mapper>