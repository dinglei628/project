<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.dao.bug_detailMapper">
    <select id="getListBug" resultType="com.demo.pojo.Bug_detail">
        SELECT d.* ,p.name
        FROM bug_detail d
        INNER JOIN bug_pro p
        ON d.projectId = p.id
        WHERE 1=1
        <if test="projectId != 0">
            AND  projectId = #{projectId}
        </if>
        ORDER  BY d.createDate DESC
	</select>
    <select id="getListBug_Por" resultType="com.demo.pojo.Bug_pro">
        select * from bug_pro
    </select>
    <insert id="insert" parameterType="com.demo.pojo.Bug_detail">
        INSERT INTO bug_detail
	(
	projectId,
	severity,
	title,
	reportUser,
	createDate
	)
	VALUES
	(
	#{projectId},
	#{severity},
	#{title},
	#{reportUser},
	now()
	);
    </insert>
</mapper>