<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zb.mapper.videoMapper">

    <insert id="videoInsert" parameterType="com.zb.pojo.Videoaddress">
        insert into commodity.videoaddress
	(
	videoTitle,
	videoAddress,
	videoTypeId
	)
	values
	(
	#{videoTitle},
	#{videoAddress},
	#{videoTypeId}
	);
    </insert>
</mapper>